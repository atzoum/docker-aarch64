Base Oracle JDK Docker Image for aarch64 (atzoum/aarch64-base-jdk)
==================================================================

- `aarch64/debian` base image
- Oracle's JDK8 for 64-bit ARM architecture
